int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    int n;
    cin>>n;
    int A[n];
    for(int i=0;i<n;i++)
    {
        cin>>A[i];
    }
    reverse(A,A+n);
    for(int i=0;i<n;i++)
    {
        cout <<A[i]<<" ";
    }
    return 0;
}
